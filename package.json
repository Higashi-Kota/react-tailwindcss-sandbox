{
  "name": "react-tailwindcss-sandbox",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*",
    "packages/features/*"
  ],
  "scripts": {
    "script:typecheck": "npx tsc --noEmit scripts/dev-watch.ts --moduleResolution node --module esnext --target esnext",
    "script:format:fix": "npx @biomejs/biome check scripts/dev-watch.ts --write",
    "clean:modules": "find packages apps -type d -name node_modules -prune -exec rm -rf {} +",
    "clean:build": "find packages apps -name '*.tsbuildinfo' -delete && find packages apps -type d -name dist -exec rm -rf {} +",
    "clean:all": "pnpm run clean:build && rm -rf dist node_modules && pnpm run clean:modules",
    "dev:watch:react-app": "node --experimental-strip-types ./scripts/dev-watch.ts react-app",
    "build:analyze": "pnpm -r build:analyze",
    "build:dev": "pnpm -r build:dev",
    "build:test": "pnpm -r build:test",
    "build:stg": "pnpm -r build:stg",
    "build:prod": "pnpm -r build:prod",
    "build:prod:app": "pnpm --filter './apps/*' -r build:prod",
    "build:prod:react-app": "pnpm --filter ./apps/react-app build:prod",
    "build:prepare": "pnpm --filter '!./apps/*' -r --workspace-concurrency=4 build:prod",
    "format": "pnpm -r format",
    "format:fix": "pnpm -r format:fix",
    "typecheck": "pnpm -r typecheck",
    "package:update": "taze",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.5",
    "@types/node": "^24.7.0",
    "concurrently": "^9.2.1",
    "knip": "^5.64.2",
    "taze": "^19.7.0",
    "typescript": "^5.9.3"
  },
  "volta": {
    "node": "24.4.0",
    "yarn": "4.9.2",
    "npm": "11.4.2",
    "pnpm": "10.14.0"
  }
}
